jQuery(document).ready(function($) {
    $('.carousel').slick({
        adaptiveHeight: true,
        slidesToShow: 1,
        dots: true,
        //variableWidth: true,
        //centerMode: true,
    });
});